{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    // DOM is needed for the web target (index.web.tsx, react-native-web).
    "lib": ["ESNext", "DOM"]
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["**/node_modules", "**/Pods", "dist"]
}
